services:
  - type: web
    name: barcode-doc-generator
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python barcode_doc_generator.py
    autoDeploy: true
    envVars:
      - key: FLASK_ENV
        value: development
